Hey Richard, for some reason as much as we tried, the defaultdomains.xml isn't getting imported to the 
database. It's in the submission, but you might have to import it manually. The report files show all 
the domains in the tables though, but it just won't import to the files. 