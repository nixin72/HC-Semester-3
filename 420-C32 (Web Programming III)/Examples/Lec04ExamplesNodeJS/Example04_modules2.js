var circle = require('./modules/circle.js');

var myRadius = 4;

console.log("The area of a circle with radius " + myRadius + " is " + circle.area(myRadius));

//console.log(`The area of a circle with radius ${myRadius} is ${circle.area(myRadius)}`);
//console.log(`The circumference of a circle with radius ${myRadius} is ${circle.circumference(myRadius)}`);
